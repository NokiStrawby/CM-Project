function [U,V, it, errs, gradnorms] = LowRankAlgoRR(rrs, A, k, max_it, err_eps, grad_eps, stop_c_type, init_t, init_w, printsteps)
randomInits = {'random', 'randfull', 'randeye'};

if ~any(strcmp(randomInits, init_t))
    % Check that the initialization mus be random
    error('k must be between 1 and the rank of A')
end

best_err = realmax();

for i = 1:rrs
    


end

